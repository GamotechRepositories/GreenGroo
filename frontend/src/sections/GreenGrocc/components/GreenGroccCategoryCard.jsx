import React from "react";
import CategoryCard from "../../../components/grocery/CategoryCard";

export function GreenGroccCategoryCard({ cat }) {
  return <CategoryCard cat={cat} />;
}

export default GreenGroccCategoryCard;
