/**
 * Subcategory pill strip — disabled site-wide for the customer frontend.
 * Kept as a no-op export so existing imports keep working.
 */
function CategoryHeaderSection() {
  return null;
}

export default CategoryHeaderSection;
