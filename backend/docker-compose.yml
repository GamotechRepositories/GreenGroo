services:
  mongodb:
    image: mongo:7
    ports:
      - "27017:27017"
    volumes:
      - mongo_data:/data/db

  backend:
    build: .
    ports:
      - "5001:5001"
    env_file: .env
    depends_on:
      - mongodb

volumes:
  mongo_data:
